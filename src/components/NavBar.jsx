const NavBar = () => {
    return (
        <div id="navbar" className="section">
        <h1>NavBar</h1>
        <p>Placeholder for the NavBar.
        </p>
        </div>
    );
};

export default NavBar;